<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or "Fight for Manhood" }}</title>

  <style>
    :root {
      --navy-dark: #0b1423;
      --navy-mid: #111827;
      --gold: #d4af37;
      --text-light: #ffffff;
      --text-muted: #b7b7b7;
    }

    body {
      margin: 0;
      font-family: "Segoe UI", Arial, sans-serif;
      background: linear-gradient(180deg, var(--navy-dark) 0%, var(--navy-mid) 100%);
      color: var(--text-light);
      line-height: 1.6;
    }

    a {
      color: var(--gold);
      text-decoration: none;
    }

    header {
      background: rgba(11, 20, 35, 0.96);
      padding: 18px 36px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 1px solid rgba(212,175,55,.25);
    }

    header h1 {
      margin: 0;
      font-size: 16px;
      letter-spacing: 2px;
      text-transform: uppercase;
      font-weight: 700;
    }

    nav a {
      margin-left: 22px;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-weight: 600;
    }

    nav a:hover {
      opacity: 0.7;
    }

    .hero {
      text-align: center;
      padding: 100px 20px 60px 20px;
      background:
        radial-gradient(900px 400px at 50% 0%, rgba(212,175,55,.15), transparent 60%),
        linear-gradient(180deg, var(--navy-dark) 0%, var(--navy-mid) 100%);
    }

    .hero h2 {
      font-size: 44px;
      margin-bottom: 14px;
      letter-spacing: 1px;
    }

    .hero p {
      font-size: 18px;
      max-width: 700px;
      margin: 0 auto 18px auto;
    }

    .button {
      display: inline-block;
      padding: 14px 28px;
      margin: 8px;
      background: var(--gold);
      color: #000;
      font-weight: bold;
      text-transform: uppercase;
      letter-spacing: 1px;
      border-radius: 5px;
      transition: all .2s ease;
    }

    .button:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 16px rgba(212,175,55,.35);
    }

    .section {
      padding: 45px 20px;
      max-width: 820px;
      margin: 0 auto;
      position: relative;
    }

    /* Gold divider line */
    .section:not(:last-of-type)::after {
      content: "";
      display: block;
      width: 60px;
      height: 2px;
      background: linear-gradient(
        to right,
        transparent,
        rgba(212,175,55,.6),
        transparent
      );
      margin: 40px auto 0 auto;
      opacity: 0.6;
    }

    .section h3 {
      font-size: 26px;
      margin-bottom: 12px;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .section p {
      margin: 0 0 14px 0;
    }

    .muted {
      color: var(--text-muted);
    }

    footer {
      text-align: center;
      padding: 40px 20px;
      background: rgba(11, 20, 35, 0.96);
      border-top: 1px solid rgba(212,175,55,.25);
      font-size: 13px;
      color: var(--text-muted);
    }
  </style>
</head>

<body>

<header>
  <h1>Fight for Manhood</h1>
  <nav>
    <a href="/">Home</a>
    <a href="/program/">Program</a>
    <a href="/apply/">Apply</a>
    <a href="/contact/">Contact</a>
  </nav>
</header>

<main>
  {{ content | safe }}
</main>

<footer>
  © 2026 Fight for Manhood
</footer>

</body>
</html>

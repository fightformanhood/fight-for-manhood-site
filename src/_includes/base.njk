<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or "Fight for Manhood" }}</title>

  <style>
    :root {
      --navy-dark: #0b1423;
      --navy-mid: #111827;
      --gold: #d4af37;
      --text-light: #ffffff;
      --text-muted: rgba(255,255,255,.75);
      --card: rgba(255,255,255,.03);
      --line: rgba(212,175,55,.25);
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: "Segoe UI", Arial, sans-serif;
      background: linear-gradient(180deg, var(--navy-dark) 0%, var(--navy-mid) 100%);
      color: var(--text-light);
      line-height: 1.65;
      font-size: 19px; /* BIGGER base text */
    }

    a { color: var(--gold); text-decoration: none; }
    a:hover { opacity: 0.85; }

    header {
      background: rgba(11, 20, 35, 0.96);
      padding: 22px 42px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 1px solid var(--line);
      position: sticky;
      top: 0;
      z-index: 10;
      backdrop-filter: blur(10px);
    }

    header h1 {
      margin: 0;
      font-size: 18px;
      letter-spacing: 2px;
      text-transform: uppercase;
      font-weight: 800;
    }

    nav a {
      margin-left: 26px;
      font-size: 13px;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-weight: 700;
    }

    nav a:hover { opacity: 0.75; }

    /* Hero (BIGGER) */
    .hero {
      text-align: center;
      padding: 120px 22px 70px 22px;
      background:
        radial-gradient(1100px 520px at 50% 0%, rgba(212,175,55,.18), transparent 60%),
        linear-gradient(180deg, var(--navy-dark) 0%, var(--navy-mid) 100%);
    }

    .hero h2 {
      font-size: 60px; /* BIG headline */
      margin: 0 0 14px 0;
      letter-spacing: 0.6px;
      line-height: 1.05;
      font-weight: 900;
    }

    .hero p {
      font-size: 22px; /* BIG hero text */
      max-width: 880px;
      margin: 0 auto 18px auto;
    }

    /* Buttons (BIGGER) */
    .button {
      display: inline-block;
      padding: 16px 34px;
      margin: 10px;
      background: var(--gold);
      color: #000;
      font-weight: 900;
      text-transform: uppercase;
      letter-spacing: 1px;
      border-radius: 8px;
      transition: all .2s ease;
      font-size: 14px;
    }

    .button:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 26px rgba(212,175,55,.30);
    }

    /* Sections (BIGGER + wider) */
    .section {
      padding: 50px 22px;
      max-width: 980px; /* wider reading */
      margin: 0 auto;
      position: relative;
    }

    .section h3 {
      font-size: 34px; /* BIG section headers */
      margin: 0 0 14px 0;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-weight: 900;
    }

    .section p { margin: 0 0 14px 0; }

    .muted { color: var(--text-muted); }

    /* Longer gold divider between sections */
    .section:not(:last-of-type)::after {
      content: "";
      display: block;
      width: 120px;
      height: 2px;
      background: linear-gradient(to right, transparent, rgba(212,175,55,.65), transparent);
      margin: 42px auto 0 auto;
      opacity: 0.8;
    }

    footer {
      text-align: center;
      padding: 46px 22px;
      background: rgba(11, 20, 35, 0.96);
      border-top: 1px solid var(--line);
      font-size: 14px;
      color: var(--text-muted);
    }

    /* Mobile tuning */
    @media (max-width: 700px) {
      header { padding: 18px 18px; }
      nav a { margin-left: 14px; }
      .hero { padding: 90px 18px 55px 18px; }
      .hero h2 { font-size: 44px; }
      .hero p { font-size: 19px; }
      .section h3 { font-size: 28px; }
      body { font-size: 18px; }
    }
  </style>
</head>

<body>

<header>
  <h1>Fight for Manhood</h1>
  <nav>
    <a href="/">Home</a>
    <a href="/program/">Program</a>
    <a href="/register/">Apply</a>
    <a href="/contact/">Contact</a>
  </nav>
</header>

<main>
  {{ content | safe }}
</main>

<footer>
  © 2026 Fight for Manhood
</footer>

</body>
</html>

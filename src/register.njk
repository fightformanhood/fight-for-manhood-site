---
title: "Register"
description: "Register for the Fight for Manhood 10-week program."
layout: "base.njk"
permalink: "/register/"
---

<section class="section">
  <div class="container">
    <div class="hero">
      <div class="hero__meta">
        <span class="pill">KNOXVILLE • WEDNESDAYS • 6:00 PM • NO COST</span>
        <span class="pill pill--muted">10 WEEKS • 2 HOURS/NIGHT</span>
      </div>

      <h1 class="hero__title">
        Register for <span class="accent">Fight for Manhood</span>
      </h1>

      <p class="hero__subtitle">
        You’re not signing up for information. You’re stepping into a process that forges real men.
      </p>

      <div class="card">
        <h2 class="card__title">Reserve your spot</h2>
        <p class="card__subtitle">
          Fill this out and we’ll follow up with the location + what to expect for Week 1.
        </p>

        <!-- Netlify Forms -->
        <form name="ffm-register"
              method="POST"
              data-netlify="true"
              netlify-honeypot="bot-field"
              action="/success/">

          <input type="hidden" name="form-name" value="ffm-register" />

          <!-- Honeypot -->
          <p style="display:none;">
            <label>Don’t fill this out: <input name="bot-field" /></label>
          </p>

          <div class="form-grid">
            <div class="field">
              <label for="name">Full Name</label>
              <input id="name" name="name" type="text" autocomplete="name" required />
            </div>

            <div class="field">
              <label for="email">Email</label>
              <input id="email" name="email" type="email" autocomplete="email" required />
            </div>

            <div class="field">
              <label for="phone">Phone (text is fine)</label>
              <input id="phone" name="phone" type="tel" autocomplete="tel" />
            </div>

            <div class="field">
              <label for="age">Age Range</label>
              <select id="age" name="age">
                <option value="">Select…</option>
                <option>18–24</option>
                <option>25–34</option>
                <option>35–44</option>
                <option>45–54</option>
                <option>55–64</option>
                <option>65+</option>
              </select>
            </div>

            <div class="field field--full">
              <label for="notes">Anything we should know?</label>
              <textarea id="notes" name="notes" rows="4" placeholder="Questions, schedule conflicts, etc."></textarea>
            </div>
          </div>

          <div class="actions">
            <button class="btn btn--primary" type="submit">Submit Registration</button>
            <a class="btn btn--ghost" href="/">Back to Home</a>
          </div>

          <p class="fineprint">
            Confidential. Men only. No cost. High challenge — high support.
          </p>
        </form>
      </div>
    </div>
  </div>
</section>

<style>
  /* Safe “layout glue” styles (won’t wreck your global design) */
  .section { padding: 5rem 0; }
  .container { max-width: 980px; margin: 0 auto; padding: 0 1.25rem; }
  .hero__meta { display:flex; gap:.75rem; flex-wrap:wrap; justify-content:center; margin-bottom:1.25rem; }
  .hero__title { text-align:center; margin: 0 0 1rem; }
  .hero__subtitle { text-align:center; max-width: 52ch; margin: 0 auto 2rem; opacity:.9; }
  .card { max-width: 760px; margin: 0 auto; padding: 2rem; border-radius: 18px; border: 1px solid rgba(255,255,255,.08); background: rgba(0,0,0,.18); backdrop-filter: blur(10px); }
  .card__title { margin: 0 0 .25rem; }
  .card__subtitle { margin: 0 0 1.5rem; opacity:.85; }
  .form-grid { display:grid; grid-template-columns: repeat(2, minmax(0,1fr)); gap: 1rem; }
  .field--full { grid-column: 1 / -1; }
  label { display:block; font-weight: 600; margin-bottom: .4rem; }
  input, select, textarea {
    width:100%;
    padding:.8rem .9rem;
    border-radius: 12px;
    border: 1px solid rgba(255,255,255,.12);
    background: rgba(0,0,0,.25);
    color: inherit;
    outline: none;
  }
  input:focus, select:focus, textarea:focus { border-color: rgba(212,175,55,.55); box-shadow: 0 0 0 3px rgba(212,175,55,.12); }
  .actions { display:flex; gap: .9rem; flex-wrap:wrap; margin-top: 1.25rem; }
  .fineprint { margin-top: 1rem; opacity:.75; font-size: .95rem; }
  @media (max-width: 720px){
    .form-grid { grid-template-columns: 1fr; }
    .card { padding: 1.25rem; }
    .section { padding: 3.5rem 0; }
  }
</style>
